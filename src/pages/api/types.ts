export interface Store {
  zip_code: string;
  name: string;
  address: string;
  phone_number: string;
  fax: string;
  is_available: string;
  location_lat: number;
  location_lng: number;
  id: string;
}
